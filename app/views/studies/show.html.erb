<header class="technique__header controls">
  <h1 class="technique__heading controls__heading"><%= @study.pretty_print %></h1>

  <div class="controls__container">
    <%= link_to "Study this", study_study_path(@study), class: "button" %>
  </div>
</header>

<dl>
  <dt>Number of techniques</dt>
  <dd>
    <%= pluralize @study.techniques.count, "technique" %>
  </dd>

  <dt>Number of times studies</dt>
  <dd>
    <%= pluralize @study.scores.count, "time" %>
  </dd>

  <dt>Average score</dt>
  <dd>
    <%= @study.average_score %>%
  </dd>

  <dt>Last study</dt>
  <dd>
    <% if @study.scores.present? %>
      <%= timestamp @study.scores.last %>
    <% else %>
      Never studied this
    <% end %>
  </dd>
</dl>

<h2>Statistics</h2>

<p>Some stats will appear here at some point</p>
